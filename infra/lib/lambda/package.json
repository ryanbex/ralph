{
  "name": "@ralph/lambda-handlers",
  "version": "0.0.1",
  "private": true,
  "main": "index.js",
  "scripts": {
    "build": "esbuild connect.ts disconnect.ts sendLogs.ts --bundle --platform=node --target=node20 --outdir=dist"
  },
  "dependencies": {
    "@aws-sdk/client-apigatewaymanagementapi": "^3.0.0",
    "@aws-sdk/client-dynamodb": "^3.0.0",
    "@aws-sdk/lib-dynamodb": "^3.0.0"
  },
  "devDependencies": {
    "@types/aws-lambda": "^8.10.134",
    "@types/node": "^20.11.0",
    "esbuild": "^0.20.0",
    "typescript": "~5.3.3"
  }
}
